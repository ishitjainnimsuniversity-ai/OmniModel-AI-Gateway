version: '3.8'

services:
  omnimodel-gateway:
    build: .
    container_name: omnimodel_gateway
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8000
      - HOST=0.0.0.0
